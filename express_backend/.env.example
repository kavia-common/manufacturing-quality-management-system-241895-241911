# Express backend configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# MongoDB (from mongo_database container)
# Required:
MONGODB_URL=mongodb://<user>:<pass>@<host>:<port>/<db>?authSource=admin
MONGODB_DB=myapp
